import styled from 'styled-components';

/**
 * Component to display page titles.
 */
const Title = styled.h1`
  font-size: 1.5em;
  text-align: center;
  color: black;
`;

/** @component */
export default Title;
